# Original (already in your venv, listed here for a clean fresh install too)
fastapi
uvicorn[standard]
httpx
pydantic
pydantic-settings
python-dotenv
faster-whisper
numpy
scipy
sounddevice

# Auth (Phase 1)
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
# Synchronous Postgres driver for the meeting store (app/meetings/pg_store.py)
# when MEETING_STORE_BACKEND=postgres. The auth DB uses asyncpg (async); the
# meeting store is called synchronously by SessionRegistry, so it needs a sync
# driver. Only imported when that backend is selected — local dev (sqlite) does
# not need it installed.
psycopg2-binary>=2.9
passlib[bcrypt]>=1.7
pyjwt[crypto]>=2.9
email-validator>=2.2
aiosmtplib>=3.0

# OAuth (Phase 4) — no extra packages, uses httpx already listed above

# LiveKit video call + per-participant transcription (Phase 6)
livekit>=0.19
livekit-api>=0.8
webrtcvad>=2.0.10

mcp
httpx

# goldenset accuracy benchmark (scripts/accuracy_benchmark.py) — decodes
# phone-recorded .m4a/.mp3 clips down to PCM16 for the ASR pipeline.
# Needs ffmpeg on PATH; not required for the app itself to run.
pydub>=0.25